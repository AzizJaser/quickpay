# Maven build output
target/

# IntelliJ IDEA (machine-specific; don't commit IDE settings)
.idea/
*.iml
*.iws
*.ipr

# OS junk
.DS_Store

# Logs
*.log
# rotated files are named e.g. wallet-service.log.2026-08-13.0.gz, so *.log misses them
logs/
# Phase 8 load-test run data — regenerated by seed.sh, never committed
scaffolding/load-test/wallets.json
